trigger:
  - master

jobs:
  - job: Windows
    pool:
      vmImage: windows-latest
    
    steps:
      - powershell: |
          Set-ExecutionPolicy -Force Bypass
          .\main.ps1
        displayName: Run `main.ps1`